# Moirai reverse proxy.
#
# На старте — только health-endpoint: удобно проверять снаружи, жив ли стек.
# Позже сюда приедут вебхуки и HTTPS (Фаза 6), когда/если понадобятся.

:80 {
	respond /health `ok
` 200
	respond 404
}
