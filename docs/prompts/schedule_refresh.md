# Routine: schedule_refresh

**Расписание:** каждый час с 09:00 до 22:00
**Выход:** `outputs/schedule.md`
**Вход:** `inbox.md`

TODO (Фаза 5): написать промпт. Промпт должен:

1. Прочитать `inbox.md`;
2. Найти все `EVENT` и `SLOT`, которые ещё не в прошлом;
3. Нормализовать естественный язык в конкретные даты и времена
   (зная текущую дату и день недели);
4. Повторяющиеся `SLOT` (`пн-пт 9-13`) развернуть в отдельные строки
   на 7 дней вперёд;
5. Отсортировать по возрастанию даты;
6. Сохранить в строго машиночитаемом формате:
   `YYYY-MM-DD HH:MM[-HH:MM] | TYPE | описание`
   (формат описан в [DATA_FORMATS.md](../DATA_FORMATS.md#outputsschedulemd)).

Важно: формат парсится регуляркой в боте, не отклоняться от схемы
строки. Никаких заголовков-секций, никаких эмодзи, ничего лишнего.

Если в inbox ничего нового относительно предыдущей версии
`schedule.md` — routine может просто не перезаписывать файл
(no-op), чтобы не триггерить бот.

Черновик в процессе — не использовать до закрытия Фазы 4.
